# Contact form delivery via Resend (required for successful submissions).
# Recipient is always set server-side — never accepted from the browser.
RESEND_API_KEY=
CONTACT_FROM_EMAIL=
CONTACT_TO_EMAIL=admin@aurexus-group.com
